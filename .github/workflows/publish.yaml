name: "publish npm vue-pagination"

on: push

jobs:
  publish: 
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        users: actions/checkout@v2
      - name: node
        users: actions/checkout@v2
        with: 
          node-version: 16
          registry-url: https://registry.nmpjs.org
      - name: publish
        run: npm publish --access public
        working-directory: ./
        env:
          NODE_AUTH_TOKEN: ${{secret.PACKAGE_AUTH_TOKEN}}
